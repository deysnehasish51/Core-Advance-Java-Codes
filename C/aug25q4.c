#include<stdio.h>

int main()
{
    int length=4;
    int breadth=5;

    printf("Area of the Rectangle is:%d\n",length*breadth);

    int side=5;

    printf("Area of the Square is : %d",side*side);
    return 0;
}